{
  "name": "pink-ward",
  "version": "0.1.0",
  "description": "Pink Ward desktop workspace shell",
  "main": "main.js",
  "author": "Pink Ward",
  "license": "UNLICENSED",
  "private": true,
  "scripts": {
    "prestart": "node build-tools/apply-dev-icon.js",
    "start": "electron .",
    "test": "node --test tests/*.test.js",
    "runtime:build": "powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts/build-python-runtime.ps1",
    "dist": "node scripts/build-windows.js",
    "dist:dir": "node scripts/build-windows.js --dir"
  },
  "devDependencies": {
    "electron": "^31.7.7",
    "electron-builder": "^24.13.3"
  }
}
